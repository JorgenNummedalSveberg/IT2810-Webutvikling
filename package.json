{
  "name": "prosjekt-3",
  "version": "1.0.0",
  "dependencies": {
    "concurrently": "^5.3.0"
  },
  "scripts": {
    "start": "concurrently --kill-others \"cd backend && node index.js\" \"cd frontend && npm start\"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}
